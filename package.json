{
  "name": "k3lpoke-marketplace",
  "version": "0.1.0",
  "description": "K3lPoke marketplace — core infrastructure and apps",
  "author": "K3lPoke",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix apps/twitch-clips-manager",
    "build": "npm run build --prefix apps/twitch-clips-manager"
  }
}
